{
  "name": "hunghsuan-personal-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy",
    "generate:og": "node scripts/generate-social-images.mjs",
    "preview:cloudflare": "npm run build && wrangler dev",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "wrangler": "^4.114.0"
  }
}
